{
  "name": "proxy-render-app",
  "version": "1.0.0",
  "description": "Proxy Render App",
  "type": "module",
  "scripts": {
    "start": "pxrender -p $PORT ."
  },
  "dependencies": {
    "proxy-render": "^1.0.5"
  }
}
